[project]
name = "vlezeapp"
version = "0.1.0"
description = "VLESS VPN Manager with GTK4/Libadwaita interface"
requires-python = ">=3.10"

[project.scripts]
vlezeapp = "main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
